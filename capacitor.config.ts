import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.p05',
  appName: 'p05',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
